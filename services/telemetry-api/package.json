{
  "name": "telemetry-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./scripts/check-syntax.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply kaan-astra-telemetry"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
